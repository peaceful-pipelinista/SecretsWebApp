<div class="jumbotron centered text-center">
  <div class="container">
    <i class="fas fa-key fa-6x"></i>

    <% if (typeof user !== "undefined") { %>
    <h1 class="display-3">Hello, <%= user.name %>! 🌼</h1>
    <p class="lead">Your email: <%= user.username %></p>
    <% } else { %>
    <h1 class="display-3">You have discovered my secret</h1>
    <% } %>

    <!-- Secret content -->
    <p class="lead">Batman is my hero 🦇</p>
  </div>

  <hr />

  <a href="/logout" class="btn btn-light btn-lg" role="button">Logout</a>
  <a href="/" class="btn btn-dark btn-lg" role="button">Home</a>
</div>
